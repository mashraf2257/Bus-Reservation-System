#include <iostream>
using namespace std;
#pragma once
#include "route.h"
class bus
{
private:
    long long bus_id;
    string bus_driver;
    int bus_capacity;
    route Route;

public:
    void setbus_driver(string b_d);
    string getbus_driver();

    void setbus_id(long long b_i);
    long long getbus_id();

    void setbus_capacity(int b_c);
    int getbus_capacity();

    void setRoute(route& r);
    route getroute();

    bus(string b_d, int b_c, route &r);
    ~bus();
};