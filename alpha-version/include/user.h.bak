#include <iostream>
#include "database.h"
using namespace std;
#pragma once

class user
{
private:
long long user_id;
string user_phonenumber; // string because the number may have +or-
string user_name;
string user_email;
string user_password; // string to include numbers and charachters
public:
    void setname_user( string u_n);
    string getname_user() ;

    void setemail_user( string u_e);
    string getemail_user() ;

    void setpassword_user( string u_p);
    string getpassword_user() ;

    void setphonenumber_user( string u_phn);
    string getphonenumber_user() ;

    void setid_user(long long u_id);
    long long getid_user() ;

    user(string u_n, string u_e, string u_p, string u_phn);
    ~user();

}; 
